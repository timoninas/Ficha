import XCTest
@testable import RevoletraUserDefaultsKeys

final class RevoletraUserDefaultsKeysTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RevoletraUserDefaultsKeys().text, "Hello, World!")
    }
}
